[build-system]
requires= ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name="task-manager"
version = "1.0.0"
requires-python = ">=3.10"

[project.scripts]
expenses = "task_manager.main:main"