[build-system]
requires= ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name="task-manager"
version = "1.0.0"
requires-python = ">=3.10"

[project.scripts]
tasks = "task_manager.main:main"